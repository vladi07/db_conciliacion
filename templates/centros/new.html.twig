{% extends 'base.html.twig' %}

{% block title %}
    New Centro|{{ parent() }}
{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center">
        <div class="card">
            <div class="card-body">
                <h2 class="font-weight-bold">Datos del Nuevo Centro</h2>
                <a href="{{ path('centros_index') }}" class="btn btn-primary">
                    <i class="bi bi-arrow-return-left"></i>
                    Volver
                </a>
                <br>
                <br>
                {{ form(formCentro) }}
            </div>
        </div>
    </div>
{% endblock %}